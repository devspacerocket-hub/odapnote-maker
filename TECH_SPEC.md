# odapnotemaker 기술 명세서 (Tech Specification)

## 1. 개요 (Overview)
`odapnotemaker`는 사용자가 업로드한 문제 이미지에서 텍스트(문제) 영역을 자동으로 인식 및 크롭하고, 스캔 필터를 적용하여 깔끔한 형태의 A4 규격 PDF로 출력할 수 있도록 돕는 웹 애플리케이션입니다. 외부 서버나 무거운 AI 모델 없이 브라우저 내장 API만을 활용하여 성능을 최적화하고 보안을 강화한 것이 특징입니다.

## 2. 사용 기술 스택 (Tech Stack)
* **Core:** React 19, TypeScript, Vite
* **Styling & UI:** Tailwind CSS, Lucide React (아이콘)
* **Image Processing:** HTML5 Canvas API, JavaScript (Vanilla 픽셀 조작)
* **PDF Export:** `jspdf` (PDF 문서 생성), `html2canvas` (DOM 요소 캡처 및 이미지화)

---

## 3. 핵심 기술 구현 명세 (Core Features & Implementations)

### 3.1. 자동 크롭 (Auto-Crop) 및 문제 영역 인식
서버로 이미지를 전송하지 않고 브라우저의 **HTML5 Canvas API**를 사용하여 로컬에서 픽셀 단위 분석을 수행합니다.

* **처리 과정 (`services/geminiService.ts`):**
  1. **해상도 스케일링:** 연산 속도 최적화를 위해 원본 이미지를 최대 800px 이하로 축소하여 Canvas에 그립니다.
  2. **그레이스케일(Grayscale) 변환:** `getImageData`로 추출한 RGB 픽셀 값을 명도(밝기) 수치로 변환합니다.
  3. **임계값(Threshold) 판별:** 밝기가 특정 임계값(예: 140) 이하인 픽셀을 '어두운 픽셀(글자/그림 등)'로 간주합니다.
  4. **밀집도 히스토그램 분석:** X축(열)과 Y축(행) 기준으로 어두운 픽셀이 얼마나 존재하는지 누적 카운트합니다.
  5. **바운딩 박스(Bounding Box) 추출:** 노이즈(얼룩)를 무시하기 위한 최소 밀집도 기준을 설정한 뒤, 상/하/좌/우에서부터 탐색하여 텍스트가 존재하는 경계 좌표를 찾아냅니다.

### 3.2. 스캔 필터 및 그림자 제거 (Scan Filter & Shadow Removal)
휴대폰 촬영 시 발생하는 불균일한 조명과 회색 배경을 제거하여 인쇄 시 잉크를 절약하고 가독성을 높입니다.

* **그림자 제거 (Background Normalization):**
  * 이미지를 일정한 크기의 격자(Grid, 예: 32x32)로 분할합니다.
  * 각 격자 내에서 가장 밝은 픽셀을 해당 영역의 '배경(종이) 색'으로 추정합니다.
  * 이 배경색을 기준으로 원본 픽셀의 명도를 평탄화하여 불균일한 조명을 보정합니다.
* **흑백 스캔 필터 (Contrast Enhancement):**
  * 픽셀의 대비(Contrast)를 강하게 증폭시키고, 옅은 회색은 완전한 흰색(255)으로 날려버려 깔끔한 흑백 스캔본처럼 만듭니다.

### 3.3. PDF 레이아웃 렌더링 및 출력
* **가상 A4 렌더링 (`PrintLayout.tsx`):**
  * 실제 A4 비율(210x297mm)을 가진 가상의 DOM 요소를 생성하고, CSS `transform: scale()`을 이용해 사용자의 모니터 크기에 맞게 반응형으로 보여줍니다.
* **그리드 시스템:** 
  * 1페이지당 1문제(1x1), 2문제(2x1), 4문제(2x2) 등 다양한 레이아웃을 제공합니다. 1x1 레이아웃의 경우 이미지가 화면을 과도하게 덮지 않도록 최대 높이를 절반(50%)으로 제한하는 로직이 적용되어 있습니다.
* **jsPDF 생성:**
  * 사용자 설정(여백, 제목, 노트 필기 공간 등)에 따라 jsPDF 객체에 렌더링된 요소들을 삽입하고 클라이언트 단에서 즉시 PDF 파일로 다운로드합니다.

---

## 4. 앱 패키징 및 성능 관점 (Performance & App Packaging)
현재 사용된 **HTML5 Canvas 기반의 픽셀 처리 로직**은 다음과 같은 아키텍처적 이점을 가집니다.

1. **완벽한 오프라인 동작 (Offline First):** 외부 API 의존성이 전혀 없으므로, PWA(Progressive Web App), Electron, Capacitor, React Native WebView 등을 통해 네이티브 앱으로 패키징할 경우 **네트워크 연결 없이도 모든 이미지 처리 로직이 100% 정상 작동**합니다.
2. **높은 성능과 낮은 오버헤드:** 무거운 딥러닝 기반 AI 모델(TensorFlow.js 등)을 로드하는 대신 단순 수학적/통계적 픽셀 배열(Array) 순회 알고리즘을 사용하므로, 메모리 점유율이 낮고 구형 모바일 기기에서도 매우 빠르게 동작합니다. 캔버스를 800px로 리사이징 후 연산하기 때문에 성능 병목이 발생하지 않습니다.
3. **보안 (Privacy):** 사용자가 찍은 문제집 사진이나 시험지가 외부 서버로 전송되지 않아 프라이버시가 완벽하게 보호됩니다.

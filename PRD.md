# 📄 제품 요구사항 정의서 (PRD) - 오답노트 메이커

## 1. 제품 개요 (Product Overview)
**오답노트 메이커**는 수험생들이 자신이 틀린 문제 사진을 업로드하여 빠르고 쉽게 고품질의 PDF 오답노트를 생성할 수 있도록 돕는 웹 기반 서비스입니다.
가장 큰 특징은 **서버 전송 없이 로컬 브라우저 환경에서 100% 동작**하여 사용자의 개인정보와 데이터를 안전하게 보호하며, 오프라인 수준의 빠른 처리 속도를 제공한다는 점입니다.

## 2. 타겟 고객 (Target Audience)
* 수능, 논술, 공무원, 자격증 등 각종 시험을 준비하는 수험생
* 태블릿이나 스마트폰으로 캡처/촬영한 문제들을 깔끔하게 인쇄용 문서로 정리하고 싶은 학생
* 자신의 학습 데이터(시험지 사진 등)를 상업용 서버에 업로드하는 것을 꺼리는 보안 민감형 사용자

## 3. 핵심 가치 (Core Values)
* **보안 및 프라이버시 (Privacy-First)**: 외부 AI API나 서버를 경유하지 않고, 사용자의 기기(브라우저) 자원만 사용하여 처리합니다.
* **편의성 (Ease of Use)**: 복잡한 기능 설정 없이 직관적인 스크롤과 클릭/드래그만으로 최적의 결과물을 유도합니다. 
* **최적화된 출력물 (Print-Optimized)**: 전용 스캐너 앱 수준의 이미지 보정(그림자 제거, 여백 자르기, 흑백 필터) 기능과 A4 사이즈에 최적화된 다중 레이아웃 기능을 제공합니다.

## 4. 주요 기능 요구사항 (Key Features)

### 4.1. 이미지 업로드 및 로컬 분석
* **상호작용 지원**: 드래그 앤 드롭 존(Zone) 및 파일 탐색기를 통한 이미지 파일 업로드 지원.
* **순수 로컬 픽셀 분석 (`geminiService.ts`)**:
  * HTML5 Canvas API를 활용해 이미지의 픽셀 데이터를 직접 순회합니다.
  * 명도(Brightness) 임계값 알고리즘을 통해 빈 여백을 찾아내고, 텍스트가 밀집된 핵심 문제 영역만을 도출(Bounding Box 추출)해냅니다.

### 4.2. 이미지 뷰어 및 사용자 에디터 (`ImageEditor.tsx`, `imageProcessor.ts`)
* **자동 1차 가공**: 로컬 가상 캔버스를 통해 자동으로 분석된 바운딩 박스 영역만큼 이미지를 잘라냅니다.
* **수동 편집 모달 창 제공**:
  * 사용자가 직접 자르기 영역(Crop box)의 크기 및 위치를 조정할 수 있는 8방향 리사이즈 핸들러 UI.
  * 이미지의 미세한 기울기를 교정할 수 있는 각도 회전 제어 기능.
* **스캔 필터 및 그림자 보정 옵션 (선택 가능)**:
  * **그림자 제거 로직**: 휴대폰 촬영 시 문서(종이)에 지는 불균일한 조명 얼룩과 그림자를 평탄화하는 자체 배경 보정 알고리즘 적용.
  * **스캔 대비(흑백) 필터**: 연한 회색 노이즈를 완전한 흰색으로 날리고, 글씨 색상을 증폭시켜 일반 문제집 텍스트와 같은 대비를 부여합니다.

### 4.3. 오답노트 대시보드 관리 (`Dashboard.tsx`)
* **상태 뷰어**: 분석과 처리가 완료된 문제의 결과 화면들을 썸네일 카드 뷰 형태로 렌더링.
* **추가 콘텐츠 기입**: 각 문제 하단에 텍스트를 기재할 수 있는 영역 제공. (틀린 이유 적기, 핵심 꿀팁 기재 등)
* **카드 개별 관리**: 특정 카드의 삭제 및 재수정(에디터 재진입) 기능 지원.

### 4.4. PDF 레이아웃 제어 및 내보내기 (`PrintLayout.tsx`)
* **레이아웃 세팅 옵션**: 사용자의 선호에 따라 페이지당 나열될 문제의 개수를 1개(1x1), 2개(1x2), 4개(2x2) 그리드로 선택 변경.
* **A4 규격 CSS 매핑**: 실제 출력될 브라우저 화면의 비율을 CSS를 활용해 A4 사이즈(210x297mm)와 정확히 일치하도록 매핑시킵니다.
* **고화질 PDF 변환 및 다운로드**: `html2canvas` 라이브러리로 구성된 리액트 DOM 트리를 고해상도로 캡처하고, `jspdf` 객체에 페이지별로 담아 하나의 PDF 문서로 엮어 기기에 직접 다운로드합니다.

### 4.5. 부가 기능
* **튜토리얼 안내 (`TutorialPopup.tsx`)**: 최초 방문자들의 이탈을 방지하고 프라이버시(서버 통신 없음)를 강조하기 위한 안내 표출 및 브라우저 스토리지 연동(다시 보지 않기).
* **수익화 연동 (`AdSenseUnit.tsx`)**: 트래픽에 대비하여 광고를 유연하게 삽입할 수 있는 확장 가능한 단일 애드센스 래퍼 컴포넌트 환경 구축.

## 5. 기술 스택 (Tech Stack)
* **Frontend Framework**: React 19 (Vite 번들러 기반)
* **Language**: TypeScript
* **Styling**: Tailwind CSS
* **Icons**: Lucide-React
* **Core Native API**: 브라우저 Native DOM / HTML5 Canvas API (이미지 2D 컨텍스트, getImageData 조작)
* **Third-party Libraries**:
  * `jspdf` (v2.5.1): 이미지 파일을 문서(PDF) 양식으로 묶어 파일로 인코딩
  * `html2canvas` (v1.4.1): HTML DOM 요소를 시각적으로 캡처(렌더링)하기 위한 툴

## 6. 향후 고도화 고려 사항 (Future Improvements)
* [ ] IndexedDB 연동을 매개로 한 오프라인 이전 세션 데이터(작업 중이던 리스트) 임시 복구 기능 구축.
* [ ] React-Beautiful-DND 등을 활용한 사용자의 임의 드래그 앤 드롭 문제 순서 재배치 지원.
* [ ] 서비스 워커(Service Worker)를 탑재한 완전한 PWA(Progressive Web App) 오프라인 모드 지원.
* [ ] (테스트 중) 로컬 브라우저 내에서 구동되는 WebAssembly + TensorFlow.js 기반의 가벼운 정교화 OCR/객체 인식 적용 모색.
